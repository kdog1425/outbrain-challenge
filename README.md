# Outbrain

To run locally, in server/server.js:

````
DEBUG = true;
````

cd to project's root folder
````
npm install
````
````
gulp && npm start
````

To see it live:
https://outbrain-khenprice.herokuapp.com/
